{
  "name": "nano-event-emitter",
  "version": "0.1.0",
  "description": "Minimal extendable jQuery EventEmitter",
  "main": "lib/index.js",
  "esnext:main": "src/index.js",
  "scripts": {
    "build": "babel ./src --out-dir ./lib --source-maps",
    "commit": "git-cz",
    "push": "npm run reset && npm run build && git push --follow-tags origin master && conventional-github-releaser -p angular && npm publish",
    "release": "npm run test && standard-version",
    "reset": "rimraf ./lib",
    "start": "npm run test && npm run watch",
    "test": "xo src/**/*.js && conventional-changelog-lint --from=HEAD^1",
    "watch": "npm run build -- --watch"
  },
  "config": {
    "commitizen": {
      "path": "cz-conventional-changelog-lint"
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/marionebl/nano-event-emitter.git"
  },
  "keywords": [
    "event-emitter",
    "events",
    "jquery"
  ],
  "author": "Mario Nebl <hello@mario-nebl.de>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/marionebl/nano-event-emitter/issues"
  },
  "homepage": "https://github.com/marionebl/nano-event-emitter",
  "devDependencies": {
    "babel-cli": "^6.9.0",
    "babel-plugin-transform-runtime": "^6.9.0",
    "commitizen": "^2.8.2",
    "conventional-changelog-lint": "^0.3.4",
    "conventional-github-releaser": "^1.1.2",
    "cz-conventional-changelog-lint": "^0.1.3",
    "husky": "^0.11.4",
    "rimraf": "^2.5.2",
    "standard-version": "^2.3.0",
    "xo": "^0.15.1"
  },
  "xo": {
    "esnext": true
  },
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
      "transform-runtime",
      "transform-class-properties"
    ]
  },
  "dependencies": {
    "babel-runtime": "^6.9.2"
  }
}
